import React from "react";
import styled from "styled-components";

const Notifications = () => {
  return (
    <Wrapper>
      <div>notifications: to do</div>
    </Wrapper>
  );
};

const Wrapper = styled.div``;

export default Notifications;
