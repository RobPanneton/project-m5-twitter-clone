import React from "react";
import styled from "styled-components";

const Bookmarks = () => {
  return (
    <Wrapper>
      <div>bookmarks : to do</div>
    </Wrapper>
  );
};

const Wrapper = styled.div``;

export default Bookmarks;
